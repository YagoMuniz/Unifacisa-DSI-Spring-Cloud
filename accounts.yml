server:
  port: ${PORT:8080}

eureka:
  instance:
    hostname: https://dsi-spring-configserver.herokuapp.com
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${eureka.instance.hostname}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000
