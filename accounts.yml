server:
  port: ${PORT:8080}

eureka:
  instance:
    hostname: dsi-spring-configserver.herokuapp.com
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: https://${eureka.instance.hostname}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000
